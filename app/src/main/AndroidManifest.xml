<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.MatZip.map">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_matzip"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_matzip_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstProject">

        <activity
            android:name="com.MatZip.map.LoadingActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.MatZip.map.MapActivity" />



        <activity android:name="com.MatZip.map.SearchActivity">
        </activity>

        <activity android:name="com.MatZip.map.MenuDetail">
        </activity>
        <activity android:name="com.MatZip.map.MyPageActivity">
        </activity>
    </application>

</manifest>