<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.map">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_matzip"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_matzip_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstProject">

        <activity
            android:name="com.example.map.LoadingActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MapActivity" />



        <activity android:name="com.example.map.SearchActivity">
        </activity>

        <activity android:name="com.example.map.MenuDetail">
        </activity>
        <activity android:name="com.example.map.MyPageActivity">
        </activity>
    </application>

</manifest>